name: verify-public

on:
  push:
    branches:
    - main
  pull_request:

jobs:
  verify-public:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run public verification
      run: ./scripts/verify-public.sh
